{
  "name": "staticsitegenerator",
  "version": "1.0.0",
  "description": "PHP Static Site Generator",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "secure": "snyk test --file=composer.lock && snyk monitor --file=composer.lock --org=learnbyapplication --project-name=staticsitegenerator"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "snyk": "^1.88.2"
  }
}
